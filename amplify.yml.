version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**

# Optional: Environment Variables
env:
  variables:
    REACT_APP_API_URL: 'https://your-api-url.com'
    REACT_APP_SWAGGER_URL: 'https://localhost:53933'
